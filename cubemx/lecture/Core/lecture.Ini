SIGNAL void push_button(unsigned int pulses){
	int i, j;
	
	for(i = 0;i<pulses;i++){
		PORTA = (PORTA & ~0x01);
		swatch(0.3);
		PORTA = (PORTA | 0x01);
		swatch(0.3);
	}
}
DEFINE BUTTON "Push button A.0", "push_button(1)"